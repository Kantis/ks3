name: build

on:
   pull_request:

   push:
      paths-ignore:
         - "doc/**"
         - "documentation/**"
         - "*.md"
         - "*.yml"
      branches:
         - "main"
         - "release/*"

   workflow_dispatch:


concurrency:
   group: "${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}"
   cancel-in-progress: true


permissions:
   contents: read


jobs:

   linux-tests:
      strategy:
         matrix:
            target:
               - jvmTest
               - jsIrTest
               - linuxX64Test
               # - jsLegacyBrowserTest
               # - jsLegacyNodeTest
               # - jsLegacyTest
               # - jsIrBrowserTest
               # - jsIrNodeTest
      uses: ./.github/workflows/gradle_task.yml
      with:
         runs-on: ubuntu-latest
         gradle-task: ${{ matrix.target }} --stacktrace --info


   macos-tests:
      strategy:
         matrix:
            target:
               - iosSimulatorArm64Test
               - iosX64Test
               - macosArm64Test
               - macosX64Test
               - tvosSimulatorArm64Test
               - tvosX64Test
               - watchosSimulatorArm64Test
               - watchosX64Test
      uses: ./.github/workflows/gradle_task.yml
      with:
         runs-on: macos-11
         gradle-task: ${{ matrix.target }} --stacktrace --info


   windows-tests:
      strategy:
         matrix:
            target:
               - mingwX64Test
      uses: ./.github/workflows/gradle_task.yml
      with:
         runs-on: windows-latest
         gradle-task: ${{ matrix.target }} --stacktrace --info


   gradle-check:
      needs: [ linux-tests, macos-tests, windows-tests ]
      # Run 'check' task for each OS.
      # To improve speed, only enable Kotlin/JVM (Kotlin/Native is particularly slow).
      # The other targets are tested individually above
      strategy:
         matrix:
            os: [ ubuntu-latest, macos-11, windows-latest ]
      uses: ./.github/workflows/gradle_task.yml
      name: "./gradlew check @ ${{ matrix.os }}"
      with:
         runs-on: ${{ matrix.os }}
         gradle-task: check --stacktrace --info -Pks3_enableKotlinNative=false -Pks3_enableKotlinJs=false
